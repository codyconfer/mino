# Companion to queries/demo.yaml — review requests with live github.com URLs.
# References the filter-only document in queries/no-bots.yaml by name.
name: demo-reviews
signal: github
filters: [no-bots]
params:
  query: "is:open is:pr review-requested:@me"
