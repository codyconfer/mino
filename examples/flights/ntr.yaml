# Scheduled reminders fire when this flight is served (`munin serve ntr`).
# NTR ReminderJob shares the daemon/serve notify sink (tray on daemon; desktop/terminal on serve).
name: ntr
type: flight
queries: [ntr-list]
