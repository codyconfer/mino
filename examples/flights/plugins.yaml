# Smoke flight for external stub plugins + gcx (offline). Enable plugins as needed.
name: plugins
queries:
  - gcx-status
  - kubectl-context
  - gooseai-context
  - pi-context
  - opencode-context
  - ollama-context
