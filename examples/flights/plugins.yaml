# Smoke flight for external stub plugins + gcx (offline). Enable plugins as needed.
name: plugins
type: flight
queries:
  - gcx-status
  - kubectl-context
  - gooseai-context
  - pi-context
  - opencode-context
  - ollama-context
  - argocd-context
