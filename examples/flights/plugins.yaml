# Smoke flight for external stub plugins + gcx. Every query here is offline on
# purpose: gcx-status pins view=status, so the flight needs no credentials and no
# stack. Add gcx-incidents only where a token and stack are configured.
name: plugins
type: flight
queries:
  - gcx-status
  - kubectl-context
  - ollama-context
