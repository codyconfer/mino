# Smoke flight for Lane C stubs + gcx C-0 (offline). Enable plugins as needed.
name: plugins
queries:
  - gcx-status
  - kubectl-context
  - gooseai-context
  - pi-context
  - opencode-context
  - ollama-context
