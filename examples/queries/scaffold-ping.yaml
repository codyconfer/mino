# Scaffold template sample. The scaffold package is not imported by the
# default mino binary — copy the package when creating a new plugin.
name: scaffold-ping
type: query
signal: scaffold
params: {}
